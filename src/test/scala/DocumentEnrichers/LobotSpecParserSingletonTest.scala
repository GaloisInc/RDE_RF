package DocumentEnrichers

import org.scalatest.flatspec.AnyFlatSpec
import org.scalatest.matchers.should

class LobotSpecParserSingletonTest extends AnyFlatSpec with should.Matchers {

  /*
  it should "be able to parse type" in {
    val input = "type dev_board = { Virtual, LFE5UM5G_85F_EVN, RV32M1_VEGA, None }"
    val parsedResult = LobotParserSingleton.parse(LobotParserSingleton.TypeDefParser, input)

    parsedResult.successful should be(true)
    val result = parsedResult.get
    result.name should be("dev_board")

    result.fields should have size 4
    result.fields.head should be("Virtual")
    result.fields(1) should be("LFE5UM5G_85F_EVN")
    result.fields(2) should be("RV32M1_VEGA")
    result.fields(3) should be("None")
  }

  it should "be able to parse type with newline" in {
    val input = "type dev_board = \n { Virtual, LFE5UM5G_85F_EVN, RV32M1_VEGA, None }"
    val parsedResult = LobotParserSingleton.parse(LobotParserSingleton.TypeDefParser, input)

    parsedResult.successful should be(true)
    val result = parsedResult.get
    result.name should be("dev_board")

    result.fields should have size 4
    result.fields.head should be("Virtual")
    result.fields(1) should be("LFE5UM5G_85F_EVN")
    result.fields(2) should be("RV32M1_VEGA")
    result.fields(3) should be("None")
  }

  it should "be able to parse type with newline and spaces" in {
    val input = "type dev_board = \n { Virtual, LFE5UM5G_85F_EVN, RV32M1_VEGA, None }"
    val parsedResult = LobotParserSingleton.parse(LobotParserSingleton.TypeDefParser, input)

    parsedResult.successful should be(true)
    val result = parsedResult.get
    result.name should be("dev_board")

    result.fields should have size 4
    result.fields.head should be("Virtual")
    result.fields(1) should be("LFE5UM5G_85F_EVN")
    result.fields(2) should be("RV32M1_VEGA")
    result.fields(3) should be("None")
  }

   */

}
